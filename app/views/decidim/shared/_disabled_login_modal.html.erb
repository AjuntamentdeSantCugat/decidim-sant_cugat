<% #Disabled login modal now the process has finished %>
<% ppp = ParticipatoryProcessPicker2022.new(nil) %>

<div class="form__wrapper">
  <%= link_to(t("verify_wo_registration.login_modal.col1_only_verify_btn"),
              Decidim::VerifyWoRegistration::Engine.routes.url_helpers.new_verification_path({
                component_id: ppp.component_id,
                redirect_url: ppp.process_url || URI(request.original_fullpath).path,
                votable_gid: "gid"
              }), class: "button button__lg button__secondary") %>
</div>
